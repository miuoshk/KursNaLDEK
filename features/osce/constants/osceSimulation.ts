/** Próg zaliczenia stacji (łącznie z obu „tasków” / pytań). */
export const OSCE_SIM_PASS_THRESHOLD = 0.6;

/** Czas na całą stację (sekundy) — ustalić po Mockup Day. */
export const OSCE_STATION_DURATION_SECONDS = 15 * 60;

/** Losujemy do N topików na stację. */
export const OSCE_TOPICS_PER_STATION = 2;

/** Pytań na wybrany topik w symulacji. */
export const OSCE_QUESTIONS_PER_TOPIC = 2;
