/** Wspólna lista kolumn `questions` dla OSCE (bez kolumny `extra`). */
export const OSCE_QUESTIONS_COLUMNS =
  "id, text, options, correct_option_id, explanation, image_url, is_active, question_type, timer_seconds, correct_order, learning_outcome, hotspots, drill_questions, identify_mode";
